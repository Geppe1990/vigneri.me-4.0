import React from "react"
import { graphql } from "gatsby";
import Menu from "../components/menu"

export default () => {
	return (
        <React.Fragment>
            <Menu />
        </React.Fragment>
	)
}